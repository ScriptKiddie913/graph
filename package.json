{
  "name": "graph-intel-monolith",
  "private": true,
  "version": "1.0.0",
  "description": "Single-service Graph Intel deployment",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install --include=dev && npm --prefix frontend run build && python3 -m pip install --no-cache-dir -r backend/requirements.txt",
    "start": "npm --prefix backend start"
  }
}
